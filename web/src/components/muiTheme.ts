import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#227c31",
      contrastText: "#FFF",
    },
    secondary: {
      main: "#fd5d4b",
      contrastText: "#333",
    },
  },
});

export default theme;
